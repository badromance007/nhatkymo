<div class="wrapper_with_padding">
  <h2>Đăng nhập</h2>
  <p class="alert alert-success notice">(<strong>Note:</strong> Nếu bạn có nick facebook hay google plus thay vì đăng nhập bạn hãy đăng nhập trực tiếp bằng cách click vào "Đăng nhập với Facebook hay Google")</p>


  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email, required: false, autofocus: true %>
      <%= f.input :password, label: "Mật khẩu", required: false %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Đăng nhập", class: "button" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>

</div>